import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { Text, View } from 'react-native';

export default function CadCli(props){
    return(
        <View>
            <Text>Esse é o componente CadCli </Text>
        </View>
    );
}