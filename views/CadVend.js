import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { Text, View } from 'react-native';

export default function CadVend(props){
    return(
        <View>
            <Text>Esse é o componente CadVend </Text>
        </View>
    );
}